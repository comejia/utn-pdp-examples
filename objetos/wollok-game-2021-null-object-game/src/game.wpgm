import wollok.game.*
import gameUI.*

program wgame {
	game.title("Wollok Defense")
	game.boardGround("background.png")
	game.width(20)
	game.height(15)
	game.cellSize(40)
	gameUI.configurar()
	game.start()
}
